namespace = hd_palace_crisis

# The visible controller is selected by hd_try_start_palace_crisis_effect.
hd_palace_crisis.0001 = {
	type = character_event
	title = hd_palace_crisis.0001.t
	desc = hd_palace_crisis.0001.desc
	theme = intrigue
	left_portrait = { character = root animation = personality_cynical }

	trigger = {
		has_character_flag = hd_palace_crisis_controller
		exists = global_var:hd_palace_crisis_queued
		is_alive = yes
	}
	on_trigger_fail = { hd_palace_recover_queued_effect = yes }

	immediate = {
		set_global_variable = hd_palace_crisis_started
		remove_global_variable = hd_palace_crisis_queued
		remove_global_variable = hd_palace_crisis_queue_retry_used
	}

	option = {
		name = hd_palace_crisis.0001.a
		trigger_event = { id = hd_palace_crisis.0002 days = { 20 50 } }
		ai_chance = { base = 100 }
	}
}

hd_palace_crisis.0002 = {
	type = character_event
	title = hd_palace_crisis.0002.t
	desc = hd_palace_crisis.0002.desc
	theme = intrigue
	left_portrait = { character = root animation = personality_rational }

	trigger = {
		exists = global_var:hd_palace_crisis_started
		is_alive = yes
	}
	on_trigger_fail = { hd_palace_recover_started_controller_effect = yes }

	option = {
		name = hd_palace_crisis.0002.a
		if = {
			limit = { title:k_uuii_liangzhou.holder ?= { is_alive = yes } }
			title:k_uuii_liangzhou.holder = {
				trigger_event = { id = hd_palace_crisis.0003 days = { 10 20 } }
			}
		}
		else = {
			if = {
				limit = { character:he_jin ?= { is_alive = yes } }
				character:he_jin = {
					trigger_event = { id = hd_palace_crisis.0004 days = { 10 20 } }
				}
			}
			else = {
				set_global_variable = hd_palace_he_jin_killed
				if = {
					limit = { character:yuan_shao ?= { is_alive = yes } }
					character:yuan_shao = {
						trigger_event = { id = hd_palace_crisis.0005 days = { 20 50 } }
					}
				}
				else_if = {
					limit = { character:cao_cao ?= { is_alive = yes } }
					character:cao_cao = {
						trigger_event = { id = hd_palace_crisis.0005 days = { 20 50 } }
					}
				}
				else = {
					hd_palace_purge_effect = yes
					hd_palace_crisis_cleanup_effect = yes
				}
			}
		}
		ai_chance = { base = 100 }
	}
}

hd_palace_crisis.0003 = {
	type = character_event
	title = hd_palace_crisis.0003.t
	desc = hd_palace_crisis.0003.desc
	theme = diplomacy
	left_portrait = { character = root animation = personality_rational }

	trigger = {
		exists = global_var:hd_palace_crisis_started
		this = title:k_uuii_liangzhou.holder
		is_alive = yes
	}
	on_trigger_fail = { hd_palace_continue_after_controller_death_effect = yes }

	option = {
		name = hd_palace_crisis.0003.a
		title:k_uuii_liangzhou = {
			set_variable = { name = hd_palace_liangzhou_invited value = yes }
		}
		hd_liangzhou_start_march_effect = yes
		if = {
			limit = { character:he_jin ?= { is_alive = yes } }
			character:he_jin = {
				trigger_event = { id = hd_palace_crisis.0004 days = { 20 50 } }
			}
		}
		else = {
			set_global_variable = hd_palace_he_jin_killed
			if = {
				limit = { character:yuan_shao ?= { is_alive = yes } }
				character:yuan_shao = {
					trigger_event = { id = hd_palace_crisis.0005 days = { 20 50 } }
				}
			}
			else_if = {
				limit = { character:cao_cao ?= { is_alive = yes } }
				character:cao_cao = {
					trigger_event = { id = hd_palace_crisis.0005 days = { 20 50 } }
				}
			}
			else = {
				hd_palace_purge_effect = yes
				hd_palace_try_finalize_liangzhou_effect = yes
			}
		}
		ai_chance = {
			base = 100
			modifier = { add = 200 has_trait = ambitious }
			modifier = { add = 10 has_trait = brave }
			modifier = { add = -100 has_trait = craven }
		}
	}
}

hd_palace_crisis.0004 = {
	type = character_event
	title = hd_palace_crisis.0004.t
	desc = hd_palace_crisis.0004.desc
	theme = intrigue
	left_portrait = { character = root animation = personality_coward }

	trigger = {
		exists = global_var:hd_palace_crisis_started
		this = character:he_jin
		is_alive = yes
		NOT = { exists = global_var:hd_palace_he_jin_killed }
	}
	on_trigger_fail = { hd_palace_continue_after_he_jin_loss_effect = yes }

	immediate = {
		character:zhang_rang ?= {
			if = {
				limit = { is_alive = yes }
				save_scope_as = hd_palace_killer
			}
		}
		if = {
			limit = { NOT = { exists = scope:hd_palace_killer } }
			character:zhao_zhong ?= {
				if = {
					limit = { is_alive = yes }
					save_scope_as = hd_palace_killer
				}
			}
		}
	}

	option = {
		name = hd_palace_crisis.0004.a
		set_global_variable = hd_palace_he_jin_killed
		remove_character_flag = hd_palace_crisis_controller
		if = {
			limit = { exists = scope:hd_palace_killer }
			death = {
				death_reason = death_murder
				killer = scope:hd_palace_killer
			}
		}
		else = { death = { death_reason = death_murder } }

		if = {
			limit = { character:yuan_shao ?= { is_alive = yes } }
			character:yuan_shao = {
				trigger_event = { id = hd_palace_crisis.0005 days = { 20 50 } }
			}
		}
		else_if = {
			limit = { character:cao_cao ?= { is_alive = yes } }
			character:cao_cao = {
				trigger_event = { id = hd_palace_crisis.0005 days = { 20 50 } }
			}
		}
		else = {
			hd_palace_purge_effect = yes
			if = {
				limit = {
					title:k_uuii_liangzhou = { has_variable = hd_palace_liangzhou_invited }
					title:k_uuii_liangzhou.holder ?= { is_alive = yes has_character_flag = hd_palace_liangzhou_marching }
				}
				title:k_uuii_liangzhou.holder = { hd_palace_try_finalize_liangzhou_effect = yes }
			}
			else = { hd_palace_crisis_cleanup_effect = yes }
		}
		ai_chance = { base = 100 }
	}
}

hd_palace_crisis.0005 = {
	type = character_event
	title = hd_palace_crisis.0005.t
	desc = hd_palace_crisis.0005.desc
	theme = war
	left_portrait = { character = root animation = personality_bold }

	trigger = {
		exists = global_var:hd_palace_crisis_started
		exists = global_var:hd_palace_he_jin_killed
		NOT = { exists = global_var:hd_palace_purge_completed }
		is_alive = yes
		OR = { this = character:yuan_shao this = character:cao_cao }
	}
	on_trigger_fail = { hd_palace_resolve_purge_recipient_failure_effect = yes }

	option = {
		name = hd_palace_crisis.0005.a
		hd_palace_purge_effect = yes
		if = {
			limit = {
				title:k_uuii_liangzhou = { has_variable = hd_palace_liangzhou_invited }
				title:k_uuii_liangzhou.holder ?= { is_alive = yes has_character_flag = hd_palace_liangzhou_marching }
			}
			title:k_uuii_liangzhou.holder = { hd_palace_try_finalize_liangzhou_effect = yes }
		}
		else = {
			if = {
				limit = { character:yuan_shao ?= { is_alive = yes } }
				character:yuan_shao = {
					trigger_event = { id = hd_palace_crisis.0006 days = { 20 50 } }
				}
			}
			else = { hd_palace_crisis_cleanup_effect = yes }
		}
		ai_chance = { base = 100 }
	}
}

hd_palace_crisis.0006 = {
	type = character_event
	title = hd_palace_crisis.0006.t
	desc = hd_palace_crisis.0006.desc
	theme = diplomacy
	left_portrait = { character = root animation = personality_bold }

	trigger = {
		this = character:yuan_shao
		is_alive = yes
		exists = global_var:hd_palace_purge_completed
	}
	on_trigger_fail = { hd_palace_crisis_cleanup_effect = yes }

	option = {
		name = hd_palace_crisis.0006.a
		create_title_and_vassal_change = {
			type = granted
			save_scope_as = hd_palace_stable_sili_change
			add_claim_on_loss = no
		}
		title:k_uuii_sili = {
			change_title_holder_include_vassals = {
				holder = root
				change = scope:hd_palace_stable_sili_change
			}
		}
		resolve_title_and_vassal_change = scope:hd_palace_stable_sili_change
		title:e_han.holder = {
			try_start_diarchy = regency
			set_diarch = root
			set_diarchy_swing = 100
		}
		remove_character_flag = hd_palace_crisis_controller
		hd_palace_crisis_cleanup_effect = yes
		ai_chance = { base = 100 }
	}
}

# Hidden final-arrival node.  It is called by the travel plan, not by
# on_domicile_moved, so intermediate provinces cannot grant司隶 prematurely.
hd_palace_crisis.0007 = {
	type = character_event
	hidden = yes
	trigger = {
		is_alive = yes
		has_character_flag = hd_palace_liangzhou_marching
	}
	on_trigger_fail = { hd_palace_liangzhou_march_cancel_effect = yes }
	immediate = {
		add_character_flag = hd_palace_liangzhou_arrived
		hd_palace_try_finalize_liangzhou_effect = yes
	}
}

# Sent to every landed Liangzhou vassal (including sub-vassals) after the main
# army begins its march.
hd_palace_crisis.0010 = {
	type = character_event
	title = hd_palace_crisis.0010.t
	desc = hd_palace_crisis.0010.desc
	theme = diplomacy
	left_portrait = { character = root animation = personality_rational }

	trigger = {
		is_alive = yes
		has_variable = hd_palace_liangzhou_inviter
		title:k_uuii_liangzhou = { has_variable = hd_palace_liangzhou_invited }
		var:hd_palace_liangzhou_inviter = { is_alive = yes }
	}
	on_trigger_fail = { remove_variable = hd_palace_liangzhou_inviter }

	immediate = {
		var:hd_palace_liangzhou_inviter = { save_scope_as = hd_liangzhou_march_leader }
	}

	option = {
		name = hd_palace_crisis.0010.a
		set_location = { location = title:b_uuii_p_787.title_province stick_to_location = yes }
		if = {
			limit = { exists = domicile }
			domicile = { move_domicile = title:b_uuii_p_787.title_province }
		}
		add_opinion = {
			target = scope:hd_liangzhou_march_leader
			modifier = obedience_opinion
			years = 15
		}
		if = {
			limit = { NOT = { has_trait = loyal } }
			add_trait = loyal
		}
		add_character_flag = hd_palace_liangzhou_followed
		remove_variable = hd_palace_liangzhou_inviter
		ai_chance = {
			base = 45
			modifier = { add = 30 opinion = { target = scope:hd_liangzhou_march_leader value >= 50 } }
			modifier = { add = 20 has_trait = loyal }
			modifier = { add = 15 has_trait = brave }
			modifier = { add = -30 has_trait = craven }
			modifier = { add = -40 opinion = { target = scope:hd_liangzhou_march_leader value <= -30 } }
		}
	}

	option = {
		name = hd_palace_crisis.0010.b
		remove_variable = hd_palace_liangzhou_inviter
		ai_chance = { base = 55 }
	}
}

# A canceled march must not leave the palace chain waiting forever.  If the
# eunuch purge has already happened, go directly to the stable Yuan branch;
# otherwise the scheduled purge event will make the same choice.
hd_palace_crisis.0008 = {
	type = character_event
	hidden = yes
	immediate = { hd_palace_liangzhou_march_cancel_effect = yes }
}

# Historical 189.5.13 transition. Titles and direct vassals are transferred
# before death so vanilla succession law never decides the Han inheritance.
hd_palace_crisis.0090 = {
	type = character_event
	hidden = yes
	trigger = {
		this = character:liu_hong_3
		is_alive = yes
		has_character_flag = hd_liu_hong_189_death_scheduled
		NOT = { has_character_flag = hd_liu_hong_189_death_completed }
	}
	immediate = {
		remove_character_flag = hd_liu_hong_189_death_scheduled
		add_character_flag = hd_liu_hong_189_death_completed
		remove_character_modifier = hd_liu_hong_timeline_protection

		if = {
			limit = { character:liu_bian ?= { is_alive = yes } }
			every_held_title = { add_to_list = hd_liu_hong_held_titles }
			every_vassal = { add_to_list = hd_liu_hong_direct_vassals }
			create_title_and_vassal_change = {
				type = granted
				save_scope_as = hd_liu_hong_succession_change
				add_claim_on_loss = no
			}
			every_in_list = {
				list = hd_liu_hong_held_titles
				change_title_holder = {
					holder = character:liu_bian
					change = scope:hd_liu_hong_succession_change
				}
			}
			every_in_list = {
				list = hd_liu_hong_direct_vassals
				limit = { NOT = { this = character:liu_bian } }
				change_liege = {
					liege = character:liu_bian
					change = scope:hd_liu_hong_succession_change
				}
			}
			resolve_title_and_vassal_change = scope:hd_liu_hong_succession_change
		}
		if = { limit = { NOT = { has_trait = ill } } add_trait = ill }
		death = { death_reason = death_ill }
	}
}

hd_palace_crisis.0015 = {
	type = character_event
	title = hd_palace_crisis.0015.t
	desc = hd_palace_crisis.0015.desc
	theme = intrigue
	left_portrait = { character = root animation = personality_bold }

	trigger = {
		is_alive = yes
		has_character_flag = hd_palace_liangzhou_settled
		exists = title:h_china.holder
	}
	on_trigger_fail = { hd_palace_crisis_cleanup_effect = yes }

	option = {
		name = hd_palace_crisis.0015.a
		if = {
			limit = { character:liu_xie ?= { is_alive = yes } }
			create_title_and_vassal_change = {
				type = granted
				save_scope_as = hd_palace_deposition_change
				add_claim_on_loss = no
			}
			title:h_china = {
				change_title_holder = { holder = character:liu_xie change = scope:hd_palace_deposition_change }
			}
			title:e_han = {
				change_title_holder = { holder = character:liu_xie change = scope:hd_palace_deposition_change }
			}
			resolve_title_and_vassal_change = scope:hd_palace_deposition_change
		}
		# Deposition immediately makes Dong Zhuo the fixed warlord-regent.
		hd_force_dong_regency_effect = yes
		add_character_flag = hd_palace_deposition_aftermath
		add_character_flag = hd_palace_ding_lu_branch_open
		character:cao_cao ?= {
			if = {
				limit = {
					is_alive = yes
					has_character_flag = hd_cao_personal_story_established
					NOT = { has_character_flag = hd_cao_early_story_started }
				}
				add_character_flag = hd_cao_early_story_started
				trigger_event = { id = hd_cao_chain.0001 days = { 20 50 } }
			}
		}
		if = {
			limit = {
				character:ding_yuan ?= { is_alive = yes }
				character:lv_bu ?= { is_alive = yes }
			}
			character:ding_yuan = {
				if = {
					limit = { character:lv_bu = { NOT = { father = character:ding_yuan } } }
					adopt = character:lv_bu
				}
			}
			character:lv_bu = {
				set_employer = character:ding_yuan
				add_character_flag = hd_ding_yuan_adoptive_son
			}
		}
		trigger_event = { id = hd_palace_crisis.0020 days = { 3 7 } }
		character:ding_yuan ?= { trigger_event = { id = hd_palace_crisis.0021 days = 30 } }
		if = {
			limit = {
				character:yuan_shao ?= {
					is_alive = yes
					has_character_flag = hd_yuan_personal_story_established
					NOT = { has_character_flag = hd_yuan_early_story_started }
				}
			}
			character:yuan_shao = {
				add_character_flag = hd_yuan_early_story_started
				set_variable = { name = hd_palace_deposition_regent value = root }
				set_global_variable = {
					name = hd_anti_regent_pending_leader
					value = this
				}
				trigger_event = { id = hd_palace_crisis.0016 days = { 20 50 } }
			}
		}
		else = { hd_anti_regent_select_fallback_leader_effect = yes }
		ai_chance = { base = 100 }
	}
}

# The second emperor and Empress He are killed off-screen a few days after
# the deposition.  This is intentionally hidden so the public chain cannot
# pause on a second player-facing choice.
hd_palace_crisis.0020 = {
	type = character_event
	hidden = yes
	trigger = {
		is_alive = yes
		this = character:dong_zhuo_2
		has_character_flag = hd_palace_deposition_aftermath
	}
	immediate = {
		character:liu_bian ?= {
			if = {
				limit = { is_alive = yes }
				death = { death_reason = death_murder killer = root }
			}
		}
		character:he_huang_hou ?= {
			if = {
				limit = { is_alive = yes }
				death = { death_reason = death_murder killer = root }
			}
		}
	}
}

# A visible confrontation is kept separate from the hidden murders so the
# historical timing remains legible while the outcome is deterministic.
hd_palace_crisis.0021 = {
	type = character_event
	title = hd_palace_crisis.0021.t
	desc = hd_palace_crisis.0021.desc
	theme = intrigue
	left_portrait = { character = root animation = personality_bold }

	trigger = {
		this = character:ding_yuan
		is_alive = yes
		character:dong_zhuo_2 = {
			is_alive = yes
			has_character_flag = hd_palace_deposition_aftermath
			has_character_flag = hd_palace_ding_lu_branch_open
		}
		character:lv_bu = {
			is_alive = yes
			father = character:ding_yuan
			has_character_flag = hd_ding_yuan_adoptive_son
		}
	}
	on_trigger_fail = { hd_ding_lu_branch_abort_effect = yes }

	option = {
		name = hd_palace_crisis.0021.a
		trigger_event = { id = hd_palace_crisis.0022 days = { 1 3 } }
		ai_chance = { base = 100 }
	}
}

# Lu Bu accepts Dong Zhuo's bargain, kills Ding Yuan, and transfers the whole
# Bingzhou grant (including its direct county) to Dong.
hd_palace_crisis.0022 = {
	type = character_event
	title = hd_palace_crisis.0022.t
	desc = hd_palace_crisis.0022.desc
	theme = intrigue
	left_portrait = { character = root animation = personality_bold }

	trigger = {
		this = character:ding_yuan
		is_alive = yes
		character:dong_zhuo_2 = {
			is_alive = yes
			has_character_flag = hd_palace_ding_lu_branch_open
		}
		character:lv_bu = {
			is_alive = yes
			father = character:ding_yuan
			has_character_flag = hd_ding_yuan_adoptive_son
		}
	}
	on_trigger_fail = { hd_ding_lu_branch_abort_effect = yes }

	option = {
		name = hd_palace_crisis.0022.a
		every_vassal = {
			limit = { is_alive = yes is_landed = yes }
			add_to_list = hd_ding_yuan_direct_vassals
		}
		every_held_title = { add_to_list = hd_ding_yuan_titles }
		create_title_and_vassal_change = {
			type = granted
			save_scope_as = hd_ding_yuan_transfer
			add_claim_on_loss = no
		}
		# Transfer the kingdom first so its title vassals follow before Ding Yuan's
		# remaining personal titles are processed.
		if = {
			limit = { has_title = title:k_uuii_bingzhou }
			title:k_uuii_bingzhou = {
				change_title_holder_include_vassals = {
					holder = character:dong_zhuo_2
					change = scope:hd_ding_yuan_transfer
				}
			}
		}
		every_in_list = {
			list = hd_ding_yuan_titles
			limit = { this != title:k_uuii_bingzhou }
			change_title_holder_include_vassals = {
				holder = character:dong_zhuo_2
				change = scope:hd_ding_yuan_transfer
			}
		}
		# Administrative title transfer does not reliably rewrite every direct
		# character liege. Reattach any remainder without flattening sub-vassals.
		every_in_list = {
			list = hd_ding_yuan_direct_vassals
			limit = {
				is_alive = yes
				is_landed = yes
				NOT = { liege = character:dong_zhuo_2 }
			}
			change_liege = {
				liege = character:dong_zhuo_2
				change = scope:hd_ding_yuan_transfer
			}
		}
		resolve_title_and_vassal_change = scope:hd_ding_yuan_transfer
		character:lv_bu = {
			remove_character_flag = hd_ding_yuan_adoptive_son
			set_employer = character:dong_zhuo_2
			set_location = { location = title:b_uuii_p_867.title_province stick_to_location = yes }
		}
		character:dong_zhuo_2 = {
			if = {
				limit = { character:lv_bu = { NOT = { father = character:dong_zhuo_2 } } }
				adopt = character:lv_bu
			}
		}
		death = { death_reason = death_murder killer = character:lv_bu }
		character:dong_zhuo_2 = {
			remove_character_flag = hd_palace_ding_lu_branch_open
			remove_character_flag = hd_palace_deposition_aftermath
		}
		ai_chance = { base = 100 }
	}
}

# All invitation responses converge here.  Li Ru's name is only used by the
# localization when he is alive; the effect itself has an anonymous fallback.
hd_palace_crisis.0023 = {
	type = character_event
	title = hd_palace_crisis.0023.t
	desc = hd_palace_crisis.0023.desc
	theme = war
	left_portrait = { character = root animation = personality_bold }

	trigger = {
		is_alive = yes
		has_character_flag = hd_current_han_regent
		has_character_flag = hd_anti_regent_invitations_complete
		exists = global_var:hd_anti_regent_league_leader
		global_var:hd_anti_regent_league_leader = {
			has_variable = hd_anti_regent_target
			var:hd_anti_regent_target = root
		}
	}

	option = {
		name = hd_palace_crisis.0023.a
		if = {
			limit = { character:yuan_shao ?= { is_alive = yes } }
			set_relation_rival = { target = character:yuan_shao reason = rival_claimed_throne }
		}
		character:cao_cao ?= {
			if = {
				limit = { is_alive = yes }
				set_location = { location = title:d_chenliu.title_capital_county.title_province stick_to_location = yes }
				domicile ?= { move_domicile = title:d_chenliu.title_capital_county.title_province }
			}
		}
		hd_move_dong_core_to_changan_effect = yes
		remove_character_flag = hd_anti_regent_invitations_complete
		if = {
			limit = {
				exists = global_var:hd_anti_regent_league_leader
				global_var:hd_anti_regent_league_leader = { is_alive = yes }
			}
			global_var:hd_anti_regent_league_leader = {
				add_character_flag = hd_anti_regent_war_ready
				trigger_event = { id = hd_anti_regent.0004 days = { 1 3 } }
			}
		}
		ai_chance = { base = 100 }
	}
}

# The on_death callback runs before title succession.  One day later the real
# Sili holder is landed and can safely inherit both the warlord government and
# the Han regency without invalidating an active anti-regent war.
hd_palace_crisis.0024 = {
	type = character_event
	hidden = yes
	trigger = {
		is_alive = yes
		is_landed = yes
		has_character_flag = hd_han_regent_succession_pending
		this = title:k_uuii_sili.holder
		exists = title:e_han.holder
		is_vassal_or_below_of = title:e_han.holder
	}
	on_trigger_fail = {
		if = {
			limit = { is_alive = yes }
			remove_character_flag = hd_han_regent_succession_pending
		}
		if = {
			limit = {
				exists = title:e_han.holder
				title:k_uuii_sili.holder ?= {
					is_alive = yes
					is_landed = yes
					is_vassal_or_below_of = title:e_han.holder
					NOT = { has_character_flag = hd_han_regent_succession_pending }
				}
			}
			title:k_uuii_sili.holder = {
				add_character_flag = hd_han_regent_succession_pending
				trigger_event = { id = hd_palace_crisis.0024 days = 1 }
			}
		}
		else = {
			if = {
				limit = { exists = global_var:hd_anti_regent_league_leader }
				global_var:hd_anti_regent_league_leader = { hd_anti_regent_failure_effect = yes }
			}
			else = {
				title:e_han.holder ?= {
					end_diarchy = yes
					remove_trait = kuzhu_puppet_ruler
				}
			}
		}
	}
	immediate = {
		remove_character_flag = hd_han_regent_succession_pending
		save_scope_as = hd_han_regent_successor
		hd_force_warlord_effect = yes
		add_trait = kuzhu_empire_overseer
		add_character_flag = hd_current_han_regent
		title:e_han.holder = {
			add_trait = kuzhu_puppet_ruler
			try_start_diarchy = regency
			designate_diarch = scope:hd_han_regent_successor
			set_diarch = scope:hd_han_regent_successor
			set_diarchy_swing = 100
		}
		if = {
			limit = { exists = global_var:hd_anti_regent_league_leader }
			global_var:hd_anti_regent_league_leader = {
				save_scope_as = hd_anti_regent_new_leader
				set_variable = { name = hd_anti_regent_target value = scope:hd_han_regent_successor }
				confederation ?= {
					set_variable = { name = hd_anti_regent_target value = scope:hd_han_regent_successor }
				}
				scope:hd_han_regent_successor = {
					set_variable = { name = hd_anti_regent_leader value = scope:hd_anti_regent_new_leader }
				}
				if = {
					limit = {
						has_character_flag = hd_anti_regent_invitations_dispatched
						NOT = { has_variable = hd_anti_regent_pending_invites }
						NOT = { has_character_flag = hd_anti_regent_war_ready }
						NOT = { has_character_flag = hd_anti_regent_first_war_started }
						NOT = { has_character_flag = hd_anti_regent_second_war_started }
					}
					scope:hd_han_regent_successor = {
						add_character_flag = hd_anti_regent_invitations_complete
						trigger_event = { id = hd_palace_crisis.0023 days = 1 }
					}
				}
			}
		}
	}
}

hd_palace_crisis.0016 = {
	type = character_event
	title = hd_palace_crisis.0016.t
	desc = hd_palace_crisis.0016.desc
	theme = intrigue
	left_portrait = { character = root animation = personality_bold }

	trigger = {
		this = character:yuan_shao
		is_alive = yes
		has_variable = hd_palace_deposition_regent
		exists = global_var:hd_anti_regent_pending_leader
		global_var:hd_anti_regent_pending_leader = root
	}
	on_trigger_fail = {
		remove_variable = hd_palace_deposition_regent
		if = {
			limit = {
				is_alive = no
				exists = global_var:hd_anti_regent_pending_leader
				global_var:hd_anti_regent_pending_leader = root
			}
			hd_anti_regent_select_fallback_leader_effect = yes
		}
		else = {
			if = {
				limit = {
					exists = global_var:hd_anti_regent_pending_leader
					global_var:hd_anti_regent_pending_leader = root
				}
				hd_anti_regent_clear_pending_dispatch_effect = yes
			}
			hd_palace_crisis_cleanup_effect = yes
		}
	}

	option = {
		name = hd_palace_crisis.0016.a
		remove_variable = hd_palace_deposition_regent
		remove_character_flag = hd_palace_crisis_controller
		# Only the deposition branch sends Yuan Shao away from Sili.  The
		# earlier "风平浪静？" branch deliberately leaves him there.
		hd_yuan_move_to_bohai_effect = yes
		hd_palace_crisis_cleanup_effect = yes
		ai_chance = { base = 100 }
	}
}
