namespace = hd_liu_bei_aftermath

# Legacy post-Yellow-Turban Liu Bei events intentionally removed.
# This inert namespace stub overwrites the previous Anxi/Du You chain while
# leaving the namespace reserved for save/debug safety until the new chain is built.
