namespace = hd_submission

# Intentionally no trigger: losing independence/capacity/government while queued
# must not prevent cleanup. Eligibility belongs inside the effect, not outside it.
hd_submission.0001 = {
    type = character_event
    hidden = yes
    immediate = {
        remove_character_flag = hd_submission_followup_pending
        hd_submission_dispatch_effect = yes
    }
}
