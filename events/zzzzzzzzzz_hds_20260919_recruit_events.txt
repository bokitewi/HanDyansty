# HanDyansty southern-story patch. Base: 9230e81ef7ee657daab8b85b2b61633da76c7814
# Additive late-loaded override; no GitHub writes.

namespace = hds_nr
hds_nr.1 = {
    type = character_event
    title = hds_nr.1.t
    desc = hds_nr.1.desc
    theme = war
    left_portrait = { character = root animation = personality_bold }
    trigger = {
        exists = character:yuan_shu
        this = character:yuan_shu
        is_alive = yes
        is_landed = yes
        has_title = title:d_nanyang
        exists = capital_province
        has_character_flag = hds_nr_nanyang_seen
        NOT = { has_character_flag = hds_nr_army_paid }
    }
    on_trigger_fail = { remove_character_flag = hds_nr_first_queued }
    immediate = {
        remove_character_flag = hds_nr_first_queued
        # Native regiment ID is pikemen_militia; peasant_militia is its category, NOT a regiment ID.
        spawn_army = {
            name = hds_nr_nanyang_yellow_turbans
            men_at_arms = { type = pikemen_militia men = 35949 }
            location = capital_province
            uses_supply = yes
            inheritable = yes
        }
        add_character_flag = hds_nr_army_paid
        hds_nr_recruit_first_effect = yes
        # Count from the first event's immediate execution, not the player dismissing the window.
        hds_nr_schedule_second_effect = yes
    }
    option = { name = hds_nr.1.a ai_chance = { base = 100 } }
}
hds_nr.2 = {
    type = character_event
    hidden = yes
    trigger = {
        exists = character:yuan_shu
        this = character:yuan_shu
        is_alive = yes
        has_character_flag = hds_nr_army_paid
        has_character_flag = hds_nr_second_scheduled
        NOT = { has_character_flag = hds_nr_second_fired }
    }
    on_trigger_fail = { remove_character_flag = hds_nr_second_queued }
    immediate = {
        remove_character_flag = hds_nr_second_queued
        add_character_flag = hds_nr_second_fired
        hds_nr_recruit_second_effect = yes
    }
}
