# 2026-09-16

## 刘备开局：庄园 → 冒险者营地（涿郡义从）

需求：刘备 184 开局不再持有世家/庄园头衔，改为 landless 冒险者，营地复用 mod 内已有的 `d_hd_zhuojun_rangers`（涿郡义从，capital = c_uuii_p_3562 涿郡）。

改动文件（均保持 UTF-8 **带 BOM** + **LF**）：
1. `common/bookmarks/bookmarks/00_bookmarks.txt:81-82` — 刘备 `title = d_hd_zhuojun_rangers`、`government = landless_adventurer_government`（原 `c_hd_nf_famous_h_house_111010250025033` + `celestial_government`）。
2. `history/titles/zz_hd_184_famous_noble_families.txt:1207` — 蒲边刘氏庄园 184.1.1 改为 `holder = 0`（整段简化，去掉 liege/government/effect）。
3. `history/titles/zz_hd_184_special_titles.txt` — 在 `d_hd_yingchuan_yellow_turbans` 之后新增 `d_hd_zhuojun_rangers` 历史块（holder = liu_bei，landless_adventurer_government + landless_adventurer_succession_law + create_landless_adventurer_title_history_effect + destroy_landless_title_no_dlc_effect）。
4. `common/landed_titles/zz_hd_184_special_titles.txt:238` — `d_hd_zhuojun_rangers` 去掉 `noble_family = yes` 与 `always_follows_primary_heir = yes`，加 `require_landless = yes`（对齐黄巾营地写法）。
5. 本地化 `uuii_dm_barony_missing_l_{simp_chinese,english}.yml` — `d_hd_zhuojun_rangers` / `_adj` 由「涿郡游侠 / Zhuxu Knight-Errant」改为「涿郡义从 / Zhuo Commandery Righteous Followers」（与 `zz_hd_uprising_war_l_*` 里已有 key `hd_zhuojun_rangers` 一致）。

## 复用要点（CK3 landless 营地）

- 营地（冒险者营地）标准三件套：`common/landed_titles` 里 `landless = yes` + `require_landless = yes`；`history/titles` 里 `government = landless_adventurer_government` + `succession_laws = { landless_adventurer_succession_law }` + `create_landless_adventurer_title_history_effect = yes` + `destroy_landless_title_no_dlc_effect`（无 RtP DLC 时销毁；`destroy_landless_title_no_tgp_dlc_effect` 是给 celestial 世家用的，别混）。
- 改头衔归属必须同步改 `common/bookmarks/bookmarks/00_bookmarks.txt`（`title` + `government`），否则书签与历史不一致。
- `common/on_action/zz_hd_184_adventurers_startup.txt` 已有逻辑：开局若刘备 `has_government = landless_adventurer_government` 则自动加 `camp_purpose_mercenaries`。本次改动后该分支才真正生效。
- 世家/庄园头衔（`c_hd_nf_*`，`noble_family = yes`）与冒险者营地互斥：同一角色改营地时要把庄园 holder 置 0。
