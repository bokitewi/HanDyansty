# 2026-08-24 工作日志

## 任务：长沙唐氏改为独立宗族 + 添加宗族格言

- 背景：昨日为角色唐明（tang_ming）建的 house_hd_changsha_tang 原本挂在现有唐氏宗族 dyn_111010064001002 下当分家；用户要求独立成族并加格言。
- 修改：
  - common/dynasties/zz_hd_184_new_dynasties.txt 新建独立宗族 dynasty_hd_changsha_tang（name="dynasty_hd_changsha_tang_name"、culture="jingzhou"、motto=dynasty_hd_changsha_tang_motto）
  - common/dynasty_houses/zz_hd_184_new_houses.txt 的 house_hd_changsha_tang 改挂 dynasty="dynasty_hd_changsha_tang"（脱离 dyn_111010064001002）
  - 本地化追加：简中 dynasty_hd_changsha_tang_name="长沙唐氏"、motto="他不知道怎么来的"；英文 "Changsha Tang" / "No one knows where he came from"
- 关键知识：CK3 宗族格言字段 = dynasty 定义内 `motto = 本地化键`（无引号，原版参照 game/common/dynasties/01_vanity_dynasties.txt，如 vanity_andersson 的 motto = dynn_Andersson_motto）。模组此前无 motto 先例。
- 备注：用户已把唐明简中名字本地化从"唐明"改为单字"明"（姓由宗族名"长沙唐氏"提供，显示为"唐明"？实际姓显示=宗族名），英文已同步改为 "Ming"。以后新增角色注意名字本地化只放"名"不放姓（模组惯例 name 字段=名，姓来自 dynasty_house）。
- 验证：BOM/括号/引用链（tang_ming → house_hd_changsha_tang → dynasty_hd_changsha_tang + motto 本地化）全 OK。

## 任务：长沙唐氏宗族/世家纹章

- 新建 common/coat_of_arms/coat_of_arms/zz_hd_changsha_tang_coa.txt：dynasty_hd_changsha_tang 与 house_hd_changsha_tang 两个纹章键，内容相同 = pattern_solid + blue/white/black + 乌尔内斯龙（ce_dragon_urnes_trollaskogur.dds，blue_light，depth 1.01）+ 猎犬（ce_hound.dds，yellow，scale 0.87）。
- 关键知识：① 纹章键名必须 = dynasty/house 的 key（dynasty_hd_xxx / house_xxx），用户模板给的 `coa_house_27257` 是第三方工具内部 ID，需替换；② 原版/模组无 `custom = yes` 字段（用户模板自带的第三方标记，引擎静默忽略，无害，保留原样）；③ 模组颜色用原版 colors.txt（blue/white/black/blue_light/yellow 均标准色）；④ 纹理 ce_dragon_urnes_trollaskogur.dds / ce_hound.dds 是原版 emblems，模组 template_lists 已引用可用；⑤ 家族纹章文件惯例：common/coat_of_arms/coat_of_arms/zz_*.txt（zz_ 后载覆盖优先）。

## 任务：唐明强制胡须"长颏带胡"

- "长颏带胡" = 原版 TGP 天命 DLC 中式胡须 m_beard_tgp_chinese_13（本地化键 PORTRAIT_MODIFIER_custom_beards_m_beard_tgp_chinese_13，理发店 custom_beards 池 tgp_chinese_beards 第 13 款）。
- 新建 gfx/portraits/portrait_modifiers/zz_hd_tang_ming_beard.txt：容器 hd_tang_ming_appearance（usage=game, selection_behavior=max, priority=2，仿模组 02_hd_eastern_han_clothing.txt）+ modifier hd_tang_ming_beard：
  - dna_modifiers.accessory：gene=beards、template=tgp_chinese_beards、accessory=m_beard_tgp_chinese_13、type=male
  - weight：base=0，条件 exists=this + exists=character:tang_ming + this=character:tang_ming + has_tgp_dlc_trigger=yes
- 关键知识：① CK3 强制指定具体胡须/发型 = portrait_modifier 的 dna_modifiers.accessory（mode=add, gene=beards, template=池名, accessory=具体项），原版参照 gfx/portraits/portrait_modifiers/02_all_historical_characters.txt（如查理发师 90104）；② 基因池模板名 = common/genes/04_genes_special_accessories_beards.txt 中的池键（tgp_chinese_beards index 39）；③ 中式胡须依赖 TGP DLC，weight 需带 has_tgp_dlc_trigger；④ DNA 的 beard 槽只能选池（随机），精确到款必须用 portrait modifier。

## 任务：删除"涿郡刘氏（刘封支）"世家头衔，刘封变为无地角色

- 删除 c_hd_nf_famous_h_uuii_book_house_5c0048d2f4d3 的 4 处引用：common/landed_titles/zz_hd_184_famous_noble_families.txt（定义块，capital=c_uuii_p_3562）、history/titles/zz_hd_184_famous_noble_families.txt（189.1.1 holder=liu_feng 块）、简中 uuii_dm_barony_missing_l_simp_chinese.yml 16967-68、英文 zz_hd_184_famous_noble_families_l_english.yml 222-23。
- 验证：全模组无残留引用；liu_feng 在 history/titles 无任何 holder 记录（无领地头衔 → 无地角色 ✓）；括号配对 OK。
- 备注：这类"X氏（Y支）"世家头衔是模组生成器产物，删除需同步 4 处（landed 定义/history holder/简中/英文），已确认无 events/on_action 引用。

## 任务：模组添加海峡通行 adjacencies（map_data/adjacencies.csv）

- 背景：用户提供 70 行"应通行海峡"地块对（包含 4 个三元组）；模组原有 3 条 river_large；需从 provinces.png+definition.csv 反推每对的 Through 海/湖/河省，写入 adjacencies.csv。
- 调研：vanilla 与模组 definition.csv type 列均为 "x"（引擎不用），靠名称前缀 sea_/lake_/river_ 区分水域；共 144 sea + 18 lake + 16 river；地图 11136×6848 解析为颜色键数组，逐省聚类质心/包围盒/像素数。
- 方法：质心连线法对非凸小省失效（大量假 NO_WATER），改用稳健边界法——距离变换求两省相对最近边界点→采样间隙+水域省 8-邻接两省"共享水域"交叉验证；通过湖泊分类（lake_* 视为 sea 类的水域）解决 589=lake_weiding 导致的 14 对误判。
- 工具沉淀：在 tools/ 下新增两个可复用脚本：
  - adjacency_analyzer.py：只读分析，输出 .workbuddy/adjacency_report.txt + adjacencies_new_rows.csv
  - adjacency_crop_review.py：把存疑对渲染成 A 红 B 蓝、海/湖/河着色的局部拼图到 .workbuddy/adjacency_review_sheet.png
- 决策：用户选"全部 55 + 链式补 2（1152-1177、1613-746）→ 57"方案，原文件已备份为 map_data/adjacencies.csv.bak_20260824
  - 干净 OK（√ 共享水域验证）= 52；回退（无共享水域）= 3（5399-3319 远隔 618px；671-1177 须经 1152；1648-746 须经 1613）；陆地连通 = 19 已剔除
- 写入后验证：60 数据行、Type 分布 13 river_large + 47 sea、所有 id 合法、Type↔Through 水域类型一致、终止行完整。
- 关键知识：
  1. CK3 adjacencies.csv 格式 `From;To;Type;Through;start_x;start_y;stop_x;stop_y;Comment`；Type 仅 vanilla 两种合法值：`sea` 与 `river_large`（无 plain `river`、无 `strait`、无 `lake`）；湖泊穿越也用 `sea`；坐标列可 -1（vanilla Tumasik-Johor / 模组现有 river_large 均为 -1）
  2. 通过海峡 = 海/湖/河省同时 8-邻接两地块（共享水域），是 adjacencies.csv Through 选择最可靠的判据；vanilla 范例 博斯普鲁斯 `496;5565;sea;947;...;Constantinople-Chalcedon`、克里木 `562;598;sea;8670;...;Kerch strait`
  3. 模组海峡/河渡命名空间：land=`PROVINCE_xxxx` 或中文名，sea=`sea_bohai_bay_N`（渤海湾系），lake=`lake_<名>`（卫丁湖 589、大鲁泽 1329 等），river=`river_<名>`（heshui 724、jishui 2436 等）
  4. 三元组（A B C）解读视情况而定：6157 5555 6409 / 5015 6387 3750 hub 与 chain 都合理（地图上三角形对水隔），671 1152 1177 实际是链式（671→1152→1177 两段河渡），1648 1613 746 也是链式（1648-1613 陆地已连无需 entry，1613-746 才是真河渡）

## 任务：检查"无成员空壳"宗族/家族

- 新增可复用检查脚本 tools/check_empty_dynasties.py：解析 common/dynasties、common/dynasty_houses 顶层块 + history/characters 的 dynasty_house 引用，差集求孤儿，并查三类悬空引用。完整空壳清单输出到 tools/empty_dynasty_report.txt。
- 结论（模组 10,523 宗族 / 10,739 家族）：无角色引用的 1,438 宗族 + 1,425 家族，其中：
  - 百家姓氏池（设计用途，非问题）：baijia1~504 + xin1~924 宗族、house_baijia1~504 + house_xin1~917 家族，供随机角色取姓（本地化 dynn_baijia1="赵"）
  - 刻意保留：100848（dynn_Tikanariyen）、1029001（dynn_Prydain，注释"dodge a stubborn error"）
  - 真孤儿 8 宗族 + 4 家族：① dynasty_hd_zhang_mancheng + house_hd_zhang_mancheng（张曼成角色 zhang_man_cheng 挂到了 uuii_book_house_bed19398c5c0，手工黄巾宗族没接上）；② dyn_111010026001002/002002/003002/004002 + dyn_111010819001002（曹/夏侯废弃备用宗族，无 house 无角色，dynasty 关联被注释且指向不存在的主宗）；③ uuii_book_dyn_9abdb3f599ee（刘）+ house_4da8cbcd9186、uuii_book_dyn_cdae1c314e2f + house_d867ee0b4caa、house_51e4d4ade4f3（夏侯分家名与宗族名相同，疑复制粘贴）
- 悬空引用检查全部通过；孤儿不会导致报错/崩溃。
- 关键知识：① 本模组角色统一用 `dynasty_house = "house_xxx"` 挂家族（无 dynasty 直接引用）；② 百家体系是随机姓氏池（预期为空）；③ 报告存 docs/empty_dynasty_house_report.md。
- 备注：沙箱内 python 运行含 open/compile 的脚本会被静默杀（RC=1 无输出），需 dangerouslyDisableSandbox 或用单行 -c 方式跑；rm 会触发 safe-delete 噪音，删临时文件可用 python os.remove。

## 任务：删除曹氏/夏侯氏 5 个废弃备用宗族（C2 处理）

- 用户确认删除 C2 类：dyn_111010026001002 / 002002 / 003002 / 004002（曹）、dyn_111010819001002（夏侯），定义于 uuii_peasant_historical_dynasties.txt。
- 引用点仅 2 处：① 宗族定义 common/dynasties/uuii_peasant_historical_dynasties.txt（5 块 26 行）；② 家徽 common/coat_of_arms/coat_of_arms/zz_dm_bce_generated_family_coas.txt（5 块含注释 135 行）。无 localization/脚本引用。
- 新增可复用工具 tools/remove_dyn_blocks.py：按顶层块 key 精确删除（支持嵌套括号），自动带删块上方紧邻注释行 + 压缩连续空行；用法 `python remove_dyn_blocks.py <文件> <key...>`。备份两份文件为 .bak_20260824_105623。
- 处理后验证：全模组 grep 无残留（仅旧报告文件）、两文件括号平衡 OK、check_empty_dynasties.py 重跑：宗族 10523→10518、空宗族 1438→1433、空家族不变 1425。
- 报告 docs/empty_dynasty_house_report.md 已同步（C2 标 ✅ 已删除，剩余疑似孤儿 = 3 宗族 [张曼成 + 2 book] + 4 家族）。
- 关键知识：删除 uuii_/生成体系条目时先 grep 全模组确认引用点（这类条目通常只有定义 + 家徽两处，无本地化键——name 是内嵌字符串如 "111010026000001_Cao"）；家徽文件每块上方有 `# dynasty xxx: BCE totem fallback` 注释需一并删除。

## 任务：为所有汉传承文化添加开局革新（17 项）

- 需求：全部 heritage_chinese 文化开局即拥有 校阅场/棉甲/床弩/夯土/军队征召/兵营/供水系统/炼焦工艺/药典/蚕桑业/占城稻/货币/坞堡/家兵/司南/世袭统治/庄园制度。
- 关键调研结论：
  1. 本版本革新定义在 `common/culture/innovations/`（非 technology/）；开局加革新的原版范本 = game_start.txt 的 on_game_start_after_lobby 块内 `every_culture_global + limit(has_cultural_pillar) + add_innovation`（heritage_arabic 解锁 ghilman）。
  2. 床弩/夯土/坞堡的 `innovation_catapult_chinese / innovation_motte_chinese / innovation_burhs_chinese` 只是原版基础革新的中文显示变体（asset 变体，按 heritage_chinese 触发显示），实际添加须用基础 key：innovation_catapult / innovation_motte / innovation_burhs。
  3. 模组故意用空文件覆盖了原版 game_start.txt（"Deliberate compatibility override"，saas_game_start.txt 接管 situation 启动），因此挂钩统一走子 on_action 追加模式，本任务用 on_game_start_after_lobby（原版加 heritage 革新的代码就在该块）。
- 改动：
  - 新建 common/on_action/zz_hd_starting_innovations.txt（UTF-8 BOM、TAB 缩进）：on_game_start_after_lobby → on_actions → hd_starting_innovations_on_game_start；effect = every_culture_global { limit { has_cultural_pillar = heritage_chinese } 17 个 add_innovation } + set_global_variable = hd_starting_innovations_initialized 防重复。
  - 本地化 innovation_burhs_chinese 显示名 "坞壁" → "坞堡"（用户需求原名）。
- 覆盖：模组 26 个汉传承文化（han、yi、bai + 23 个州/区域文化 bingzhou/hd_*_state/各 zhous），运行时对所有 heritage_chinese 文化生效（含后续新增/杂交保留汉传承者）。
- 验证：17 key 全部在原版/模组存在；括号 8/8、BOM ✓、缩进全 TAB、loc BOM 保留、无命名冲突。

## 任务：修复加冕礼（coronation）活动进行到一半无提示中断

- 根因：模组是汉总转换（replace_path 替换 landed_titles/history/titles/map_data），原版地图头衔（c_byzantion、b_aachen、b_constantinople、b_krakow、b_london、b_paris、b_reims、b_vaticano、e_france、k_france、e_byzantium、b_roma、d_sunni、struggle:persian_struggle 等）全部不存在。模组为兼容把引用这些头衔的事件整段清空为 `trigger = { always = no }`（注释 `# Absent vanilla-map title links isolated` / `unsupported in the total conversion`），**过度隔离**了加冕礼的两个核心阶段控制器 `0200`（前奏开始，选主祭）与 `0205`（加冕开始，触发整条仪式链），导致仪式链根本不起动、空转后跳宴会，表现为"中途中断"。
- 方案（用户选：完整恢复仪式链）：从原版 `D:\SteamLibrary\steamapps\common\Crusader Kings III\game\events\activities\coronation_activity\` 提取被隔离事件，恢复到模组，仅隔离真正引用缺失头衔的 `trigger` 分支。
- 关键判定：CK3 中 `= title:c_byzantion` 这类**比较/条件**里引用不存在的头衔，引擎静默视为 **false（不崩溃）**；`override_background` 的 `trigger` 同理，fallback（如 NOT{c_byzantion}→temple）正确生效。只有 `title:X.holder` 这类**作用域语句**会解析成空作用域（但 klank.1010 的 trigger 要求 struggle:persian_struggle，TC 已被 replace_path 移除，事件永不触发，故无害）。因此除 `0205` 的 7 个 `triggered_desc` 背景分支改为 `always = no` 外，其余事件均**逐字恢复（verbatim）**。
- 恢复清单（已验证括号平衡 + 全部 ACTIVE）：
  - `coronation_events.0200`（前奏/选主祭，c_byzantion 背景分支隔离）— coronation_events.txt（Task 1）
  - `coronation_events_0205_audience` 触发器（恢复完整 NOR 逻辑）— coronation_events.txt
  - `coronation_events.0205`（加冕开始；7 个缺失头衔 triggered_desc → `always = no`，含 east_asian 分支保留）— coronation_events.txt
  - `coronation_events.6100/6110/6120/6122/6130/6140`（仪式链，逐字恢复）— coronation_events_6.txt
  - `coronation_events.1007`（c_byzantion 比较，逐字恢复）— coronation_events_1.txt
  - `coronation_events_klank.1010`（d_sunni/struggle 比较，逐字恢复；永不触发）— coronation_events_klank.txt
  - 6121 原本未隔离，已确认 ACTIVE；0300/0301（主祭选择）原本 ACTIVE。
- 链式验证（脚本扫全部 `id = coronation_events.X` 引用）：0205.after→6120/6110/6130/6123；6130→6131→0100(誓言)；6100→6140→6122→…→0100；0100.after=`coronation_ready_effect`（推进到宴会阶段）。**全链路目标均为 ACTIVE，无残留 always=no 死链。**
- 本地化：模组 descriptor.mod 未 replace_path 任何 localization，且不自带 coronation 本地化 → 继承原版 coronation_activity_l_*.yml（含全部 0205/6100/… 键），无需补本地化。
- 提取的逐字原版块存于 `.workbuddy/coronation_restore/`（11 个文件），可作后续比对/回滚参考。
- 验证手段备注：沙箱内 `python` 跑含 open/compile 的多行脚本会被静默杀（RC=1 无输出），需 dangerouslyDisableSandbox 或合并到单脚本；括号校验用 brace-match 而非 grep -c（grep -c 按行计数会漏多括号行）。
