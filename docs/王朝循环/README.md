# 王朝循环维护文档

整理日期：2026-09-13。对象：本 mod 的 W/H、NEG-01～20、ROY-01/08、PAR-01、ORD-09/13/19，以及共用记录、治理、调试系统。本文档随主工作空间保存，不参与游戏加载。

## 阅读入口

| 文档 | 用途 |
|---|---|
| [功能与参数总表](功能与参数总表.md) | 查二十项影响、六节点、参数、自然触发与未完成边界 |
| [维护指南](维护指南.md) | 查文件职责、调用顺序、作用域、账本、改动步骤与常见故障 |
| [调试与验收](调试与验收.md) | 新存档测试顺序、失败场景、离线检查命令和历史证据 |
| [源码文件索引](源码文件索引.csv) | 可用 Excel 打开；逐文件路径、类型、顶层符号、当前哈希及上次交付对照 |
| [符号索引](符号索引.csv) | 按事件ID、effect/trigger/value名称或中文键查文件与行号 |

CSV 是只读整理时生成的检索快照；顶层符号用文本规则提取，不是引擎解析证明。索引覆盖当前 common、events、gui 和中文本地化中含 `hd_dc` 的文本资源，并补入 2026-09-12 交付清单的文件。它表示“与系统有关”，不表示每个文件或整份文件全部由本任务制作；原生覆盖经常同时包含其他系统。未纳入英语本地化。

本次索引共286条文件记录（284份存在、2份缺失）和1131条顶层定义/中文键定位。符号索引不枚举每个变量、每个局部脚本块或所有原生符号；查询这些内容仍使用指南中的 `rg` 方法。索引整理脚本位于 `D:/codex_checks/hd_dc_docs_20260913/index.ps1`，只生成文档CSV，不生成游戏脚本。

## 先看版本边界

| 层次 | 已知事实 | 维护时如何使用 |
|---|---|---|
| 2026-09-12 交付 | 70 套离线检查通过；101 文件清单无失败；未启动游戏 | 属于该次源码快照的历史证据 |
| 2026-09-13 当前源码 | 上述 101 文件中 95 份字节相同，5 份变化，1 份缺失 | 不能把昨天的通过结果直接套到今天全部源码 |
| 当前自然调度 | 普通事件仍走人物年度脉冲；额外派系有双层十年锁；盗库年度脉冲已删除 | 以当前代码为准，旧参数批准记录用于理解演变 |
| 游戏内验证 | 本轮文档整理未启动游戏，没有新运行证明 | 由玩家按调试清单检查 |

### 与上次 101 文件验收清单的差异

| 文件 | 本次只读核对状态 |
|---|---|
| `common/scripted_effects/hd_dc_theft_pulse_effects.txt` | 已缺失，Git 显示删除；不要按旧报告宣称年度盗库检测仍在运行 |
| `common/scripted_triggers/hd_dc_faction_pulse_claimant_triggers.txt` | 哈希变化 |
| `localization/replace/simp_chinese/hd_dc_era_override_l_simp_chinese.yml` | 哈希变化 |
| `localization/simp_chinese/hd_dc_correspondence_l_simp_chinese.yml` | 哈希变化 |
| `localization/simp_chinese/hd_dc_debug_inspection_l_simp_chinese.yml` | 哈希变化 |
| `localization/simp_chinese/hd_dc_debug_l_simp_chinese.yml` | 哈希变化 |

另外，`common/on_action/hd_dc_theft_pulse_on_actions.txt` 当前已删除；该文件不在上次这份 101 文件清单内。派系十年锁、普通事件脉冲等共享文件也不全部包含在这份哈希对照中，所以“95 份相同”不是全系统无变化的结论。本次没有恢复、删除或修复这些游戏文件，也不推断其他改动的作者。

## 必须保留的交付事实

1. 盗库四处原生门按用户决定关闭；旧崩溃根因未证明，不能标记为可玩完成。
2. ROY-08 缺少执行者、处罚前依据或原生事务编号时保留待核，不猜测守约，也不自动发最高奖励。
3. 六个节点独立结案，不表示后续完整剧情已经制作；普通节点池本次制作的是三项 ORD 节点，不是另外二十个新剧情。
4. 只考虑新存档。本次文档及维护说明只面向中文；仓库已有的其他语言资源不属于本次整理成果。
5. 所有玩法修改仍在主工作空间进行。检查脚本、缓存和运行日志产物放 D 盘；此目录保存的是用户明确要求的正式维护文档。

维护文档时同时更新日期、参数和当前差异表；不要删除历史证据的日期或把“离线通过”改写为“游戏可玩”。
