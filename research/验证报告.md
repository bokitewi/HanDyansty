# 验证报告

## 实际执行结果

执行时间（UTC）：2026-09-16T00:54:15.671940+00:00。

**39项Python单元/回归测试通过，0失败。** 原始输出见 `test_results.txt`，机器可读摘要见 `verification.json`。Python源代码通过3.10语法兼容解析。

测试使用自带的合成微型模组夹具，检查读取实际字节后产生的脚本文本，不是模拟点击游戏。CLI测试确实执行了程序入口：只读预览→备份应用→再次执行无新增改动→从备份逐字节恢复。另一CLI测试验证未解决项目存在时，没有明确 `--allow-partial` 就拒绝写入。

## 覆盖的规则

| 层面 | 检验点 |
|---|---|
| 修改边界 | 非虚构、空持有者不改；受保护标题全文保持；原184年以外历史条目不变；原角色与地理定义不变 |
| 官位与县领 | 李相如先接陇西；李参成为当地县级封臣；现任历史太守保留；不拿历史县或其他郡守最后一县；高级人物不强行降为同级封臣 |
| 角色 | 缺人才建新ID和家族；同名多候选、死亡、同音不同字不误用；重复执行不重复建人 |
| 世族 | 历史族长保留；虚构族长且家族匹配时替换；家族不匹配不强制改宗；不存在世族时建立无地结构与有效县首府；待考备注不变成头衔 |
| 解析与合并 | 嵌套、注释、引号、BOM、typed color、数组；同日不同持有者暂停；同日重复相同持有者同步替换；无相邻限制的职位分组 |
| 写入与备份 | 预览不改原件；写入前新增/变更输入触发停止；SHA-256校验、原件备份、回滚；不覆盖后续编辑；拒绝越界路径及符号链接 |

## 旧角色参考文件测试

对先前的角色修复压缩包解压出的 **96个文本文件、11657个根定义块** 完成解析，耗时约1.489秒。该档案与当前仓库字节数不同；只说明解析器可读取这批旧资料，不说明当下已完整核对人物。

原用户工作簿SHA-256核对通过；51个内容行拆分为49个含姓名记录和2个出处续行，48名不同人物、38组职位、23个明确家族标签。随包规则工作簿6张表，Excel ZIP结构检查通过；其原表页值与输入逐格一致，并已渲染检查总览页。

## 没有完成的验证

**没有启动CK3，没有运行游戏原生解析器或ck3-tiger，没有取得当前完整模组完成全量分配，没有进行新存档、继承、世族UI或运行性能实测，也没有在Windows系统执行BAT入口。** Python CLI和文件处理测试在本次工作环境中执行。

本次不能报告“已替换X位当前郡守/新增Y位缺失人物”之类实际数量；这些必须等待本地完整扫描。测试通过不等于当前模组所有项目可安全分配，也不等于游戏引擎必然接受全部输入。未解决的角色身份、世族宗属和历史位置冲突按明细保留。

本包是可检查源码的本地生成/应用工具，而不是声称已经完成全部名单安置的静态覆盖包。
