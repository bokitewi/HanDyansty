#HD: 逐鹿中原(chinese_consolidation_cb)的边境劫掠式占领结算。
#复刻原版fp2_border_raid(03_fp2_wars.txt)的掠夺效果，但全部直执行——
#原版把掠夺/转让包在show_as_tooltip内仅作结算界面展示，这里需要真实落地。
#数值直接引用副本03_dlc_fp2_script_values.txt中的fp2_border_raid_*_value。

hd_chinese_consolidation_plunder_county_effect = {
	#县(title)scope执行；依赖scope:attacker。顺序同原版fp2_border_raid_damage_effect：
	#先掷建筑摧毁(成则+25金)，再降控制度/发展度，最后按(已降1点的)发展度×5给攻方金币。
	#不调用fp2_border_raid_gold_change_value——其内部检查scope:raid_province，该scope由
	#destroy_random_building_variable_effect保存，在多县循环中会跨县残留导致重复+25。
	title_province = {
		add_province_modifier = {
			modifier = recently_looted_modifier
			years = 5
		}
	}
	ordered_county_province = {
		order_by = num_buildings
		random = {
			chance = 25
			modifier = { factor = destroy_building_soldier_multiplier_value }
			modifier = { add = destroy_building_development_multiplier_value }
			custom_tooltip = destroy_random_building_tt
			destroy_random_building_variable_effect = yes
			destroy_random_building_effect = yes
			scope:attacker = { add_gold = fp2_border_raid_building_gold_value }
		}
	}
	change_county_control = fp2_border_raid_control_change_value
	change_development_level = fp2_border_raid_development_change_value
	scope:attacker = {
		add_gold = {
			value = prev.development_level
			multiply = 5
		}
	}
}
