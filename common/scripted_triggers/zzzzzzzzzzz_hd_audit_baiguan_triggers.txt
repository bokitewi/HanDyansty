# Character scope only; invoked after the GUI's optional-root boundary.
hd_audit_baiguan_available_trigger = {
    is_alive = yes
    is_ruler = yes
    OR = {
        highest_held_title_tier = tier_hegemony
        AND = {
            highest_held_title_tier = tier_empire
            is_independent_ruler = yes
        }
    }
}
