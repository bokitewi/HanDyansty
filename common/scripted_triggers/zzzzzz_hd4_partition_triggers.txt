hd4_partition_source_still_local_trigger = {
	OR = {
		NOT = {
			exists = holder
		}
		AND = {
			exists = holder.top_liege
			holder.top_liege = {
				has_character_flag = hd4_yuan_origin_member
			}
		}
	}
}
