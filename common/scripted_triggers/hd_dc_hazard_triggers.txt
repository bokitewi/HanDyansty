# Temporary numeric scopes follow vanilla 00_goverment_values' cache pattern.
# BASE is a complete native initiation probability, not a target-pool weight.
hd_dc_scheme_hazard_inputs_trigger = {
	save_temporary_scope_value_as = { name = hd_dc_hazard_raw value = $BASE$ }
	save_temporary_scope_value_as = { name = hd_dc_hazard_base value = { value = scope:hd_dc_hazard_raw min = 0 max = 100 } }
	save_temporary_scope_value_as = { name = hd_dc_hazard_multiplier value = $ACTOR$.hd_dc_scheme_hazard_multiplier_value }
}
