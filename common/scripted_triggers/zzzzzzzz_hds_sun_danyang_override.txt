# Legacy key retained for save/script compatibility. Its semantic target is now Danyang.
hds_sc_wu_control_trigger = {
	this = character:sun_ce
	has_title = title:d_danyang
	is_landed = yes
	title:d_danyang = {
		# Completion requires EVERY de jure county to be politically under Sun Ce.
		# An unheld county or a third-party holder keeps the historical Danyang phase open.
		NOT = {
			any_de_jure_county = {
				NOT = {
					holder = {
						OR = {
							this = character:sun_ce
							is_vassal_or_below_of = character:sun_ce
						}
					}
				}
			}
		}
	}
}
