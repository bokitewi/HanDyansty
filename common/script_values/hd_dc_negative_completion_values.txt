# Raw native weights, evaluated at the existing random_list/duel scope.
hd_dc_negative_completion_native_bad_value = {
	value = 0
	switch = {
		trigger = scope:hd_dc_negative_pool
		flag:grief = {
			value = "trait_compatibility(scope:deceased_pal)"
			multiply = 2 min = -30 max = 50 add = 5
			if = { limit = { scope:deceased_pal = { has_trait = loyal } } add = 10 }
		}
		flag:sad_duel = {
			value = scope:duel_value multiply = -2 add = 30
			if = { limit = { scope:sad_boi = { OR = { has_trait = arbitrary has_trait = bossy has_trait = arrogant has_trait = cynical has_trait = stubborn has_trait = disloyal } } } add = 30 }
		}
		flag:legend_duel = { value = scope:duel_value multiply = -3.5 min = -49 add = 10 }
		flag:guest = { value = scope:hd_dc_negative_guest_bad }
		flag:provisions = { value = 1 }
		flag:goliard = { if = { limit = { can_be_drunkard = yes drinks_alcohol_trigger = yes } add = 5 } if = { limit = { can_be_hashishiyah = yes } add = 5 } }
		flag:missionary = {
			value = 50
			if = { limit = { scope:suzerains_missionary = { opinion = { target = scope:suzerain value < low_positive_opinion } } } multiply = 25 }
			if = { limit = { scope:suzerains_missionary = { has_trait = zealous } } multiply = -25 }
			if = { limit = { scope:suzerains_missionary = { has_trait = depressed } } multiply = -10 }
		}
	}
	min = 0
}
hd_dc_negative_completion_native_other_value = {
	value = 0
	switch = {
		trigger = scope:hd_dc_negative_pool
		flag:grief = {
			value = 40
			if = { limit = { scope:deceased_pal = { has_trait = disloyal } } subtract = 5 }
			add = { value = "trait_compatibility(scope:deceased_pal)" min = -30 max = 50 add = 30 min = 0 }
			add = { value = "trait_compatibility(scope:deceased_pal)" multiply = 2 min = -30 max = 50 add = 25 if = { limit = { scope:deceased_pal = { has_trait = loyal } } add = 5 } min = 0 }
		}
		flag:sad_duel = {
			value = scope:duel_value multiply = 2 add = 70
			if = { limit = { scope:sad_boi = { OR = { has_trait = just has_trait = pensive has_trait = forgiving has_trait = trusting has_trait = loyal } } } add = 30 }
		}
		flag:legend_duel = {
			value = scope:duel_value multiply = 3.5 min = -49 add = 50
			if = { limit = { is_witch_trigger = no } multiply = 0.75 }
			add = { value = scope:duel_value multiply = -3.5 min = -49 add = 50 }
		}
		flag:guest = { value = scope:hd_dc_negative_guest_other }
		flag:provisions = {
			value = 1
			if = { limit = { NOT = { has_character_modifier = weak_stomach_modifier } } add = 1 }
			if = { limit = { prowess >= 2 } add = 1 }
			if = { limit = { NOR = { has_trait = cannibal any_secret = { type = secret_cannibal } } } add = 1 }
			if = { limit = { NOR = { has_trait = disfigured has_character_modifier = hunger_sickness_modifier } } add = 1 }
			if = { limit = { NOT = { has_trait = blind } } add = 1 }
			if = { limit = { NOT = { has_trait = infirm } } add = 1 }
		}
		flag:goliard = {
			value = 65
			if = { limit = { has_diplomacy_lifestyle_trait_trigger = yes } add = 5 }
			if = { limit = { has_intrigue_lifestyle_trait_trigger = yes } add = 5 }
			if = { limit = { NOT = { has_trait = lifestyle_poet } } add = 10 }
			if = { limit = { has_trait = tourney_participant } add = 10 }
			if = { limit = { is_eunuch_trigger = no NOR = { has_trait = lovers_pox has_trait = early_great_pox has_trait = great_pox has_trait = celibate has_character_flag = immune_to_disease } can_contract_disease_trigger = { DISEASE = lovers_pox } } add = 15 }
		}
		flag:missionary = {
			value = 50
			if = { limit = { scope:suzerains_missionary = { opinion = { target = scope:suzerain value >= low_positive_opinion } } } multiply = 25 }
		}
	}
	min = 0
}

hd_dc_negative_completion_guest_bad_factor_value = {
	value = 1000
	if = {
		limit = { scope:hd_dc_negative_pool_active = 1 scope:hd_dc_negative_pool_multiplier > 1 hd_dc_negative_completion_native_bad_value > 0 hd_dc_negative_completion_native_other_value > 0 }
		value = hd_dc_negative_completion_dynamic_chance_value
		multiply = hd_dc_negative_completion_native_other_value
		multiply = 1000
		divide = { value = 100 subtract = hd_dc_negative_completion_dynamic_chance_value }
		divide = hd_dc_negative_completion_native_bad_value
	}
}

# Independent evaluations avoid relying on modifier evaluation order. No draw.
hd_dc_negative_completion_dynamic_chance_value = {
	value = hd_dc_negative_completion_native_bad_value
	multiply = 100
	divide = { value = hd_dc_negative_completion_native_bad_value add = hd_dc_negative_completion_native_other_value }
	if = {
		limit = {
			save_temporary_scope_value_as = { name = hd_dc_hazard_base value = { value = hd_dc_negative_completion_native_bad_value multiply = 100 divide = { value = hd_dc_negative_completion_native_bad_value add = hd_dc_negative_completion_native_other_value } } }
			save_temporary_scope_value_as = { name = hd_dc_hazard_multiplier value = scope:hd_dc_negative_pool_multiplier }
		}
	}
	value = hd_dc_hazard_probability_value
}
hd_dc_negative_completion_dynamic_bad_factor_value = {
	value = 1000
	if = {
		limit = { scope:hd_dc_negative_pool_active = 1 hd_dc_negative_completion_native_bad_value > 0 hd_dc_negative_completion_native_other_value > 0 }
		value = hd_dc_negative_completion_dynamic_chance_value
		multiply = { value = hd_dc_negative_completion_native_bad_value add = hd_dc_negative_completion_native_other_value }
		multiply = 10
		divide = hd_dc_negative_completion_native_bad_value
	}
}
hd_dc_negative_completion_dynamic_other_factor_value = {
	value = 1000
	if = {
		limit = { scope:hd_dc_negative_pool_active = 1 hd_dc_negative_completion_native_bad_value > 0 hd_dc_negative_completion_native_other_value > 0 }
		value = 100 subtract = hd_dc_negative_completion_dynamic_chance_value
		multiply = { value = hd_dc_negative_completion_native_bad_value add = hd_dc_negative_completion_native_other_value }
		multiply = 10
		divide = hd_dc_negative_completion_native_other_value
	}
}
