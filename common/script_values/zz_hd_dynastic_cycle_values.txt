# Project-specific Dynastic Cycle balance for the 184 Han start.
# Keep the ordinary 50% threshold; only the catastrophic threshold is relaxed.
catalyst_hegemony_far_too_few_lands_value = 0.2

# HD:夺取天命阈值从51%提高到80%(同名覆盖原版10_tgp_dynastic_cycle_values.txt,
# 决议tooltip里的百分比文本读取的就是这个值,会自动更新)
claim_mandate_china_county_percentage_value = {
	value = 0.80
}

# HD:重写"宣称者已控制郡县数"统计(同名覆盖原版)。
# 原版按 holder.top_liege 计数,默认宣称者已是独立统治者;
# 本mod里天子仍在、宣称者往往还是天朝封臣(如曹操),故改为按
# "本人 / 本人封臣链(any_liege_or_above) / 本人朝贡体系(含朝贡者的国内封臣)"计数
claim_mandate_china_control_held_value = {
	value = 0
	save_temporary_scope_as = holder_temp
	title:h_china = {
		every_de_jure_county = {
			limit = {
				holder ?= {
					OR = {
						this = scope:holder_temp
						any_liege_or_above = { this = scope:holder_temp }
						is_tributary_of_suzerain_or_above = scope:holder_temp
						top_liege = { is_tributary_of_suzerain_or_above = scope:holder_temp }
					}
				}
			}
			add = 1
		}
	}
}
