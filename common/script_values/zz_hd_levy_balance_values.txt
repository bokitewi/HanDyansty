# HanDynasty levy quantity rebalance for CK3 1.19.0.6

hd_global_levy_size_reduction = {
    value = -0.30

    # These governments already have levy_size = -0.50 in HanDynasty.
    # -0.50 + -0.15 = -0.65 => 35% of base, exactly 70% of their previous 50%.
    if = {
        limit = {
            OR = {
                has_government = administrative_government
                has_government = celestial_government
                has_government = steppe_admin_government
                has_government = meritocratic_government
            }
        }
        add = 0.15
    }

    # Map-only wild-state placeholder is already levy_size = -1; do not alter its zero-levy state.
    if = {
        limit = { has_government = hd_yeren_government }
        add = 0.30
    }
}
