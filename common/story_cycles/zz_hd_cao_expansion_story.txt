# Exactly one live instance belongs to Cao. Ending the native story cancels
# its native timer; there is no chain of orphaned delayed pulse events.
hd_story_cao_expansion = {
	visible = yes
	icon = { trigger = { always = yes } reference = "gfx/interface/icons/story_cycles/story_icon_conqueror.dds" }
	on_setup = {
		set_variable = { name = hd_cao_expansion_story_region value = story_owner.var:hd_cao_expansion_region }
	}
	visualization = { custom_string_key = "HD_CAO_EXPANSION_STORY_STATUS" }
	on_owner_death = {
		story_owner = { hd_cao_expansion_owner_death_effect = yes }
		scope:story = { end_story = yes }
	}
	effect_group = {
		days = 60
		triggered_effect = {
			trigger = { story_owner = { is_alive = yes } }
			effect = {
				save_scope_as = hd_cao_expansion_tick_story
				story_owner = { hd_cao_expansion_tick_effect = yes }
			}
		}
	}
}
