# Append a sub-on_action; never replace the engine hook's shared effect block.
on_government_change = {
    on_actions = { hd_audit_dm_government_change }
}

hd_audit_dm_government_change = {
    effect = {
        dm_government_reform_on_government_change_effect = yes
    }
}
