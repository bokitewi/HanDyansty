# Active rules.
embattle_side_commander_appointor = {
    categories = { tweaks }
    default = embattle_side_commander_appointor_ai_only_except_own_troop
    embattle_side_commander_appointor_ai_only_except_own_troop = { flag = GG_can_change_rule }
    embattle_side_commander_appointor_player_exempt = { flag = GG_can_change_rule }
}
embattle_side_commander_traits_sharing = {
    categories = { tweaks }
    default = embattle_side_commander_traits_sharing_enable
    embattle_side_commander_traits_sharing_enable = { flag = GG_can_change_rule }
    embattle_side_commander_traits_sharing_disable = { flag = GG_can_change_rule }
}
embattle_side_commanders_trait_xp = {
    categories = { tweaks }
    default = embattle_side_commanders_trait_xp_enabled
    embattle_side_commanders_trait_xp_enabled = { flag = GG_can_change_rule }
    embattle_side_commanders_trait_xp_disabled = { flag = GG_can_change_rule }
}
# Old-save rule IDs retained only as compatibility data. Neither option runs tactics.
embattle_tactic = {
    categories = { }
    default = embattle_tactic_disabled
    embattle_tactic_enabled = { }
    embattle_tactic_disabled = { }
}
# Retired third-party compatibility ID. No unresolved acotw_obscured trait dependency.
embattle_acotw_compatch = {
    categories = { }
    default = embattle_acotw_compatch_disabled
    embattle_acotw_compatch_enabled = { }
    embattle_acotw_compatch_disabled = { }
}
